# Copy to config/passwords.yaml before running integration tests.
# Values must match docker-compose.yaml (test-only placeholders).
shared:
  database: 'DB_TEST_PASSWORD'
  redis: 'REDIS_TEST_PASSWORD'
